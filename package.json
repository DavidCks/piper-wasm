{
  "name": "piper-wasm",
  "version": "0.0.5",
  "description": "Spawns a webworker running a wasm build of the sherpa-onnx runtime to generate speech using piper TTS client-side",
  "author": "DavidCks",
  "module": "index.ts",
  "main": "lib/index.js",
  "type": "module",
  "license": "Apache-2.0",
  "devDependencies": {
    "bun-types": "latest",
    "webpack-cli": "^5.1.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0",
    "tslib": "^2.6.2"
  },
  "scripts": {
    "build": "bun build index.ts --outdir lib && bun build worker.js --outdir lib && bun run build:declaration",
    "build:declaration": "tsc --emitDeclarationOnly --project tsconfig.json"
  },
  "dependencies": {
    "@types/chrome": "^0.0.260",
    "@types/firefox-webext-browser": "^120.0.0",
    "@types/webextension-polyfill": "^0.10.7"
  }
}